// Temporary beta cleanup. Keep the API and confirmation dialog in agreement.
export const SITES_DELETE_CONFIRMATION = "DELETE ALL SITES";
export interface SitesDeleteResult {
  deletedSites: number;
  deletedDispatches: number;
}
